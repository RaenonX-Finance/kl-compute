apps:
  - namespace: 'KL.Compute'
    name: 'KL.PxParse'
    # Entry point
    interpreter: none
    script: './KL.PxParse.exe'
    # Execution configs
    exec_mode: 'fork'
    # Restart options
    autorestart: false
    # Others
    time: true  # Log timestamp in logs