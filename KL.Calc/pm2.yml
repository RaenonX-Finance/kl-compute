apps:
  - namespace: 'KL.Compute'
    name: 'KL.Calc'
    # Entry point
    interpreter: none
    script: './KL.Calc.exe'
    # Execution configs
    exec_mode: 'fork'
    # Restart options
    min_uptime: 30000
    max_restarts: 5
    restart_delay: 10000
    # Others
    time: true  # Log timestamp in logs