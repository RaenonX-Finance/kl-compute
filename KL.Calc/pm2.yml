apps:
  - namespace: 'KL.Compute'
    name: 'KL.Calc'
    # Entry point
    interpreter: none
    script: './KL.Calc.exe'
    # Execution configs
    exec_mode: 'fork'
    # Restart options
    autorestart: false
    # Others
    time: true  # Log timestamp in logs