apps:
  - name: 'KL.Calc'
    # Entry point
    interpreter: none
    script: './KL.Calc.exe'
    # Execution configs
    exec_mode: 'fork'
    # Others
    cron_restart: '0 0 */3 * *'  # Restart every 3 days
    time: true  # Log timestamp in logs